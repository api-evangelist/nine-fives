openapi: 3.2.0
info:
  title: Nine Fives System API
  version: '1.0'
  contact:
    name: Nine Fives
    url: https://ninefives.com
  x-generated: '2026-07-20'
  x-method: generated
  x-refined-note:
  - x-source differs across the merged source definitions and was not carried
  description: 'Operations tagged System across 2 of this provider''s published API definitions: nine-fives-attenuator-openapi.yml, nine-fives-switch-openapi.yml. Each path carries the servers of the definition it was published in.'
servers:
- url: http://{deviceIp}
  description: Device-hosted API on port 80 (default Ethernet fallback IP 192.168.0.95, USB-C 192.168.42.95)
  variables:
    deviceIp:
      default: 192.168.0.95
      description: The IP address of the attenuator device on the local network
tags:
- name: System
  description: Device status and reboot
paths:
  /api/system/status:
    get:
      operationId: getSystemStatus
      tags:
      - System
      summary: Get complete system status
      description: Returns complete system status information including device name, model, serial, connection status, and temperature.
      responses:
        '200':
          description: System status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemStatus'
    servers:
    - url: http://{deviceIp}
      description: Device-hosted API on port 80 (default Ethernet fallback IP 192.168.0.95, USB-C 192.168.42.95)
      variables:
        deviceIp:
          default: 192.168.0.95
          description: The IP address of the attenuator device on the local network
  /api/system/reboot:
    post:
      operationId: rebootSystem
      tags:
      - System
      summary: Reboot the device
      description: Reboots the device. The device will be unreachable for approximately 30 seconds.
      responses:
        '200':
          description: Reboot acknowledged
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusMessage'
    servers:
    - url: http://{deviceIp}
      description: Device-hosted API on port 80 (default Ethernet fallback IP 192.168.0.95, USB-C 192.168.42.95)
      variables:
        deviceIp:
          default: 192.168.0.95
          description: The IP address of the attenuator device on the local network
components:
  schemas:
    StatusMessage:
      type: object
      properties:
        status:
          type: string
        message:
          type: string
    SystemStatus:
      type: object
      properties:
        device:
          type: string
          description: Device name
        hostname:
          type: string
          description: System hostname
        model:
          type: string
          description: Device model identifier
        serial:
          type: string
          description: Serial number
        status:
          type: string
          description: 'Connection status: Connected via eth, Connected via usb, or Disconnected'
        temperature:
          type: number
          description: Device temperature in °C
        connectedIface:
          type: string
          description: Network interface the client is connected through (eth0 or usb0)
    SystemStatus_2:
      type: object
      properties:
        device:
          type: string
        hostname:
          type: string
        model:
          type: string
        serial:
          type: string
        status:
          type: string
        temperature:
          type: number
        connectedIface:
          type: string
x-refined-from:
- nine-fives-attenuator-openapi.yml
- nine-fives-switch-openapi.yml
